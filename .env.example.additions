# Agregar/ajustar estas líneas en tu .env (no reemplaza el .env.example de Laravel):

DB_CONNECTION=sqlite
# Borra o comenta DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD que trae Laravel por defecto

# Opcional — habilita el botón "Analizar plano" en la UI:
ANTHROPIC_API_KEY=
